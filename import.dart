// Import from dart's core libraries;
import 'dart:math';
// Import from packages
// import 'package:flutter/widget'; It still doesn't work. Until we use it in projects.
// Import from a path/directory.
import 'for_import/function.dart';
void main(){
  print(pow(3, 4));
  print(fibonacci(3));
}